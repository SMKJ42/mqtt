[package]
name = "mqtt_server"
version = "0.1.0"
edition = "2021"

[dependencies]
mqtt-core = { path = "../mqtt-core" }
mqtt-packets = { path = "../mqtt-packets" }
bytes = "1.8.0"
tokio = { version = "1.40.0", features = ["full"] }
chrono = "0.4.38"

[dev-dependencies]